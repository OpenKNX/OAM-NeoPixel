# NeoPixel ESP32 4 MB partition table
#
# The application needs more than the 64 KiB KNX partition from OGM-Common's
# standard table, so KNX uses the otherwise unused 64 KiB before 0x2A0000.
# Both KNX and OPENKNX are mapped by OpenKNX::Flash::Driver; their offsets must
# therefore be aligned to the ESP32 MMU page size (64 KiB). The unused bytes
# after each data partition are intentional MMU-page padding.
#
# This replaces the earlier unaligned NeoPixel table. It moves OPENKNX, NVS and
# the filesystem back to the standard OGM-Common offsets, so the first factory/
# USB flash must include the partition table and will require a new ETS download;
# saved OpenKNX state, Wi-Fi credentials and filesystem content are not retained.
# Application offsets remain unchanged.
#
# Name,       Type, SubType, Offset,     Size
# --------------------------------------------
phy_init,     data, phy,     0x9000,     0x1000
otadata,      data, ota,     0xE000,     0x2000
app0,         app,  ota_0,   0x10000,    0x280000
knx,          data, 0x99,    0x290000,   0x19000
openknx,      data, 0x99,    0x2B0000,   0x8000
nvs,          data, nvs,     0x2C0000,   0x10000
spiffs,       data, spiffs,  0x2D0000,   0x130000
